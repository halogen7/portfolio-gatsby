import React from "react";
import Layout from "../components/Layout/layout";

const AboutPage = () => {
	return (
		<Layout pageTitle={"About Me"}>
			<p>Hi, I'm a front-end focused web developer.</p>
			<p>This website is built using Gatsby.</p>
		</Layout>
	);
};

export default AboutPage;
